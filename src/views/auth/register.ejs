<div class="max-w-md mx-auto">
  <h1 class="text-2xl font-bold text-slate-900 mb-6 text-center">Regisztráció</h1>

  <% if (typeof errors !== 'undefined' && errors && errors.length) { %>
    <div class="bg-red-50 border border-red-200 text-red-800 rounded-lg px-4 py-3 text-sm mb-4">
      <ul class="list-disc list-inside space-y-1">
        <% errors.forEach(function(e){ %><li><%= e %></li><% }) %>
      </ul>
    </div>
  <% } %>

  <form action="/regisztracio" method="post" class="bg-white rounded-xl border border-slate-200 p-6 space-y-4">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
    <div>
      <label class="block text-sm font-medium text-slate-700 mb-1">Név</label>
      <input type="text" name="name" required value="<%= values.name || '' %>"
        class="w-full rounded-lg border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-brand" />
    </div>
    <div>
      <label class="block text-sm font-medium text-slate-700 mb-1">E-mail cím</label>
      <input type="email" name="email" required value="<%= values.email || '' %>"
        class="w-full rounded-lg border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-brand" />
    </div>
    <div>
      <label class="block text-sm font-medium text-slate-700 mb-1">Jelszó</label>
      <input type="password" name="password" required minlength="8"
        class="w-full rounded-lg border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-brand" />
      <p class="text-xs text-slate-500 mt-1">Legalább 8 karakter.</p>
    </div>
    <div>
      <label class="block text-sm font-medium text-slate-700 mb-1">Jelszó megerősítése</label>
      <input type="password" name="password2" required minlength="8"
        class="w-full rounded-lg border border-slate-300 px-3 py-2 focus:outline-none focus:ring-2 focus:ring-brand" />
    </div>
    <button type="submit" class="w-full py-2.5 rounded-lg bg-brand hover:bg-brand-dark text-white font-semibold">Fiók létrehozása</button>
  </form>

  <p class="text-center text-sm text-slate-600 mt-4">
    Van már fiókod? <a href="/belepes" class="text-brand font-semibold hover:underline">Jelentkezz be!</a>
  </p>
</div>
